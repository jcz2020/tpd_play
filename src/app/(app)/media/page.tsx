/**
 * @fileoverview This file defines the Media Management page.
 * Its primary role is to provide a user interface for creating,
 * viewing, and managing playlists.
 * @module app/(app)/media/page
 */

import { MediaManagement } from "@/components/MediaManagement";

export default function MediaPage() {
    return <MediaManagement />;
}
