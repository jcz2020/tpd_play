/**
 * @fileoverview This file defines the Settings page for the application.
 * It allows users to configure various settings, with the primary function
 * being the management of local music folder paths for library scanning.
 * @module app/(app)/settings/page
 */

import { Settings } from "@/components/Settings";

export default function SettingsPage() {
    return <Settings />;
}
